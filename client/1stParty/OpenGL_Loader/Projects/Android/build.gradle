apply from: "${rootProject.projectDir}/client/VrLib.gradle"

android {
	defaultConfig {
		externalNativeBuild {
			ndkBuild {
				targets "openglloader"
			}
		}
	}

	sourceSets {
		main {
			manifest.srcFile 'AndroidManifest.xml'
		}
	}
}
